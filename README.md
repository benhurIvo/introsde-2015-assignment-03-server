# introsde-2015-assignment-03-server

Heroku wsdl is: https://whispering-shelf-2052.herokuapp.com/ws/people?wsdl

To run the server on localhost:
Clone the server folder from github
On the terminal browse to the cloned folder
Type: 'ant install' to get the required libraries and also build the files
Type: 'ant start' to start the server
